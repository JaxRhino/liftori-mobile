/**
 * DEPRECATED — superseded by `app/(app)/lead/` directory.
 *
 * Wave 6 moved Lead into a nested stack (`lead/_layout.tsx` +
 * `lead/[id].tsx`) so detail screens push on top of the tabs with native
 * back-swipe behavior. This file no longer has a default export, so
 * expo-router ignores it. Delete once OneDrive lets us.
 */
export {};
